import type { DeepDiveChapter } from './types'

// 相邻核心组件深讲：与向量库专章同等认真的五章。
// 每章都保持诚实：讲清生产该怎么做，同时讲清本仓库做到哪一步、为什么。

export const COMPONENT_CHAPTERS: DeepDiveChapter[] = [
  {
    id: 'embedding',
    title: 'Embedding 模型选型',
    intro: 'Embedding 定义了整个系统的「语义相似观」——它认为不相关的内容，后面所有环节都救不回来。这一章讲怎么选、怎么换、怎么算账。',
    sections: [
      {
        heading: '选型维度：六个问题按顺序问',
        paragraphs: [
          '① 语言与领域：中文场景必须用中文强势模型（BGE、GTE、通用多语模型的中文子集质量差异很大）；法律、医疗等术语密集领域要做领域评测，通用榜单代表不了你的语料。',
          '② 检索任务适配：要选「为检索训练的」模型（对称/非对称检索有别——问题短、文档长，非对称检索模型对这种长度差做过优化，有的要求加 query/passage 前缀，接入时漏加前缀是常见的隐形 bug）。',
          '③ 维度与成本：维度决定存储与检索内存（百万 chunk × 1536 维 × 4 字节 ≈ 6GB 纯向量）；更高维不必然更准，但必然更贵。部分模型支持 MRL（套娃表示）可截断降维，是成本优化的新选项。',
          '④ 有效输入长度：超过模型有效长度的文本会被截断或质量衰减——这直接约束 Chunk 大小上限，两个参数必须一起定。',
          '⑤ 部署形态：API（快、数据出域）vs 自托管（数据可控、GPU 固定成本）。合规红线场景没得选，只能自托管。',
          '⑥ 榜单参考：MTEB/C-MTEB 只用来筛入围名单（前 10 进候选），最终决定必须用自家 20-50 条黄金问答实测——榜单第 1 和第 5 在你的语料上可能反转。',
        ],
      },
      {
        heading: '换模型：最贵的一类变更，怎么管理',
        paragraphs: [
          '换嵌入模型 = 全量重嵌入 + 索引重建，成本与语料规模线性。派生索引架构（见向量库专章）把它从「迁移手术」降为「重建操作」，但钱还是要花——所以换模型必须有评测数据支撑，「新模型榜单高 2 分」不构成理由。',
          '灰度姿势：新旧模型各建一个 collection，用评测集与影子流量对比 Hit@k，达标后切流量、废旧集合。绝对禁忌是新旧向量混在一个 collection——空间不同，分数即噪声。',
          '本仓库现状：模型注册表支持多 Embedding 档案、上传时可选、Ingest Trace 记录所用模型、换模型后可一键重建——灰度对比的机制地基是有的；缺的是自动化的评测对比工具（Eval 还在 CLI 阶段）。',
        ],
      },
      {
        heading: '成本模型（能给管理层看的版本）',
        paragraphs: [
          '索引成本 = chunk 数 × 平均 token × 单价，随语料增长一次性发生；重嵌入 = 再付一次。查询成本 = QPS × 查询 token × 单价，持续发生。自托管把两者都换成 GPU 固定成本 + 运维人力——语料大或 QPS 高时拐点很快到来，这笔账 PM 要会当场算。',
        ],
      },
    ],
    interviewQs: [
      { q: '嵌入模型和生成模型需要同一家吗？', a: '完全不需要，两者职责正交：嵌入管「找得到」，生成管「说得好」。本仓库把两者做成独立的模型档案（kind=embedding / chat）分别绑定，就是这个解耦的产品化表达。唯一的硬约束在嵌入侧内部：查询与索引必须同模型。' },
      { q: '怎么发现「嵌入模型不行」？', a: '症状：评测里 Hit@20 都低（不是排序问题而是根本没捞到）、同义改写命中率骤降、领域术语检索系统性失败。归因顺序：先排除切分问题（chunk 过大稀释），再做嵌入模型 AB——直接换个模型跑同一评测集，差距立现。' },
    ],
  },
  {
    id: 'chunking',
    title: 'Chunk 策略',
    intro: '切分决定系统「看世界的分辨率」，是流水线上影响质量的第一大可调参数，也是最便宜的优化起点。',
    sections: [
      {
        heading: '策略光谱：从便宜到贵',
        paragraphs: [
          '固定滑窗（字符/token 数 + 重叠）：最便宜、行为可预测，是一切评测的基线——本仓库的实现在此基础上沿 Block 结构边界切，避免跨节切断。',
          '结构感知切分：按标题/段落/表格行边界切——利用作者已经做好的信息组织，性价比最高的升级。本仓库因为有 Block 层（解析器保留 heading_path），天然就是结构感知的。',
          '语义切分：按相邻句子嵌入相似度的「断崖」找主题边界——对无结构长文本有效，但要额外付一遍嵌入成本，且边界不稳定（模型一换边界全变）。',
          '父子分层（small-to-big）：小块检索大块阅读，解耦「检索最优粒度」与「阅读最优粒度」——本仓库通过 Chunk→Block 关系真实扩展父级上下文，window / max_chars_per_side 可调；没有关系时会如实记录无扩展。',
          'LLM/Agent 切分与上下文富化（给每块生成摘要或前置全文梗概）：效果上限高、索引成本翻数倍——先证明前三档不够再上。',
        ],
      },
      {
        heading: '参数决策的方法论（不是数字）',
        paragraphs: [
          '四步法：① 看内容形态——FAQ/条款类自包含短内容用 400-800 字符，叙事/论证类用 1200-2000；② 对齐嵌入模型有效长度——chunk 上限不应超过它；③ 建 20 条黄金问答扫参数（400/800/1200/1600 各跑 Hit@5），一小时出数据；④ 有重排时可放宽粒度精度（粗召回容忍偏大 chunk，重排负责精选）。',
          '重叠（overlap）只治「答案恰好跨边界」一种病：10-15% 通常够。过大重叠 = 花钱买冗余 + 相邻块向量趋同挤占 Top-K 多样性。',
          '本仓库的实验闭环：装配页改 chunker 的 max_chars/overlap → 保存草稿 → 数据页 reprocess → Ingest Trace 看 chunk 数量变化 → 重跑同一问题对比证据。参数改动的效果全程可观察，这正是「工作台当设计沙盘」的典型用法。',
        ],
      },
      {
        heading: '常见误区清单（面试高频）',
        paragraphs: [
          '误区一「chunk 越大上下文越全」：向量是整块文本的平均语义，块越大越糊，专指性问题召回下降——上下文问题该用父子分层解决，不该用大块硬扛。',
          '误区二「所有文档一套参数」：FAQ 和年报的最优粒度差三倍。本仓库当前全库一套 chunker 配置，是已知简化——按文档类型路由不同切分参数是明确的改进方向（parse_route 已经分了类型，管道上有挂点）。',
          '误区三「切分是工程细节不用 PM 管」：切分粒度直接决定引用粒度——用户看到的「依据」是一整页还是精准两句，是产品体验决策。',
        ],
      },
    ],
    interviewQs: [
      { q: '如果只能做一个检索优化，你选什么？', a: '看评测症状：Hit@20 低 → 修切分和嵌入（召回根子上没捞到）；Hit@20 高但 Hit@5 低 → 上重排。多数团队的第一顺位其实是把切分从「盲切 500 字」升级为「结构感知」——几乎零成本，收益普遍。' },
      { q: '表格和代码怎么切？', a: '不用通用切法。表格按行切且每行带表头上下文（本仓库 tabular 路由的做法）；代码按函数/类边界切。原则同一句话：沿内容的天然语义边界切，格式不同边界不同——这也正是解析路由存在的理由。' },
    ],
  },
  {
    id: 'rerank',
    title: 'Rerank 的产品价值与依赖边界',
    intro: '重排是生产 RAG 最常见的「第二个上的模块」。本仓库已经接通 Cross-Encoder 适配器：注册可达的 /rerank 端点时真实重排；未配置时明确 fallback，方便在本地离线环境继续学习。',
    sections: [
      {
        heading: '原理一分钟：双编码器 vs 交叉编码器',
        paragraphs: [
          '召回用的双编码器（bi-encoder）把问题和文档各自独立压成向量再算距离——为了「亿级库毫秒查」牺牲了交互信息：模型看不到问题和文档的逐词对应关系。',
          '交叉编码器（cross-encoder）把「问题+文档」拼在一起过模型，逐 token 交叉注意力，能看见「问的是保修期，这段讲的是退货期」这种细粒度错位——相关性判断准一个档次，但每对都要跑一次前向，只能用在小候选集上。',
          '所以生产形态是两级漏斗：召回负责「别漏」（top 50-100 进池），重排负责「排对」（池内精排出 5-8 条）。一个模块同时优化召回率和精确率必然平庸——这是检索工程几十年的基本功，不是 LLM 时代的新发明。',
        ],
      },
      {
        heading: '产品价值：为什么生产常配',
        paragraphs: [
          '直接价值：把「答案在第 8 名」变成「答案在第 1 名」。Top-3 命中率的提升直接传导为答案质量与用户信任——用户不看 MRR，但他们能感觉到「第一条引用就是对的」。',
          '结构价值：有了重排兜底，上游可以放宽——chunk 粒度不用调到完美、召回 top_k 可以开大、混合检索两路可以都糙一点。重排是让整条流水线「各环节都不用完美」的容错层。',
          '成本价值可以干净算账：每查询 +100~300ms、+一次小模型推理费，换 Top-3 命中率提升（自家评测说话）。对比每次坏答案的人工兜底成本，盈亏平衡点很容易算——重排是少数能对管理层给出干净 ROI 的模块。',
          '选型速览：托管 API（Cohere Rerank 类）最快接入、数据出域；开源自托管（BGE-reranker 类）数据可控、要 GPU；用生成式大模型做 listwise 重排贵一个量级，先证明 cross-encoder 不够再考虑。',
        ],
      },
      {
        heading: '本仓库的实现边界（排期即产品决策）',
        paragraphs: [
          '现状：reranker 节点会读取 model_ref、candidate_k、final_k。注册可达的 /rerank 端点时 Trace 标记 rerank_live；端点不可用时标记 fallback_passthrough，证据顺序保持不变——这是依赖降级，不是伪装成已完成。',
          '为什么仍然要先扩池和评测：重排的收益要在「召回池足够大且有评测尺子」的前提下才能测量。推荐闭环是：dense/sparse top_k 提到 30~50 → 接一个本地 BGE Cross-Encoder 端点 → 输出 final_k=6 → 用黄金集对比 Hit@3/MRR 与 p95 → 收益达标才默认开启。',
        ],
      },
    ],
    interviewQs: [
      { q: '重排和「让 LLM 自己挑有用证据」有什么区别？', a: '把挑选交给生成模型 = 把噪声塞进 Prompt 并祈祷注意力正确——贵（长上下文）、不可测（挑选过程不可见）、不可控（每次可能不同）。独立重排层便宜、有分数可记录（可进 Trace 与评测）、行为可复现。凡是能在生成之前用确定性/小模型解决的，都不要留给生成模型。' },
      { q: '什么时候重排反而没用？', a: '三种情况：召回池本身没捞到答案（精排巧妇难为无米之炊）；候选池就 5 条（排来排去还是这几条）；问题是全局/聚合类（该走图或结构化查询，不是排序问题）。重排是精排层，不是万能药——先诊断（Hit@20 vs Hit@5）再开药。' },
    ],
  },
  {
    id: 'generation',
    title: '生成模型与 Groundedness（有据性）',
    intro: 'RAG 里生成模型的角色定位是「戴着镣铐的阅读理解者」：只许依据证据说话、逐句给引用。这一章讲怎么把「有据」从愿望变成机制。',
    sections: [
      {
        heading: '角色定位：知识来源 vs 语言组织者',
        paragraphs: [
          'RAG 的根本契约：知识来自检索证据，模型负责阅读、组织、表达。模型参数里的知识在这里是风险源而不是资产——它会在证据不足时「热心地」补全，这就是幻觉的主产地。',
          '所以选型权重与通用聊天场景完全不同：指令遵循（会不会乖乖只用证据、按格式标 [S#]）> 领域语言质量 > 时延（含流式）> 成本 > 通用榜单名次。一个中型模型 + 严格约束，通常好过一个旗舰模型 + 自由发挥。',
        ],
      },
      {
        heading: '把「有据」做成机制而不是愿望：四层防御',
        paragraphs: [
          '第一层 Prompt 契约：明确「仅依据以下证据回答；每个事实句标注 [S#]；证据不足时明确说不知道」。这是必要条件，但单靠它不够——模型对 Prompt 的服从是概率性的。',
          '第二层解码约束：temperature 0~0.2。知识问答要的是稳定复述，不是创意写作。本仓库 llm_generate 的 temperature 真实生效，可以现场调 0 和 1.5 对比输出方差。',
          '第三层事后校验（本仓库真实实现）：citation_repair——回答里的引用与证据对不上时，不放行自由生成的文本，降级为证据摘要。相当于「引用完整性」的最后一道闸。同理，模型端点不可用时 extractive_fallback 直接返回证据拼接——宁可给「没加工的真话」，不给「流利的编造」。',
          '第四层评测监控：引用忠实率进核心指标（见 Eval 章）。四层合起来，「有据」才从团队愿望变成系统性质。',
        ],
      },
      {
        heading: '工程细节的产品含义',
        paragraphs: [
          '时延：生成是全链路时延大头，流式输出（首 token 时间）是体感优化第一杠杆——本仓库未实现流式，是体验侧最明显的欠账之一。',
          '成本：生成费用 = 上下文（top_k × chunk 大小 + 指令）+ 输出（max_tokens 封顶）。上下文部分被检索参数直接放大——context_builder 的 token 预算已经是可调的成本闸门。',
          '模型分级：简单 FAQ 复述用小模型、复杂推理用大模型，按意图路由分流——本仓库当前提供启发式 intent_router 基线；它可运行但仍需用 Intent Macro-F1 评测后再替换为分类模型。',
        ],
      },
    ],
    interviewQs: [
      { q: '怎么向业务方证明「我们的答案有据」？', a: '三件套：每个答案的 [S#] 引用当场可点开对证据；每次运行有 Evidence Capsule 可下载复核；评测报告里有引用忠实率曲线。把「信任我们」变成「自己验证」，这是 RAG 相对纯生成的根本产品优势——放弃了引用体系的 RAG 等于放弃了自己最强的卖点。' },
      { q: '证据互相矛盾时模型该怎么办？', a: '这是产品规则不是模型自由裁量：预先定优先级（新版本 > 旧版本、官方文档 > 论坛），在 metadata 里落字段、在 Prompt 里给规则、在答案里显式说明「不同来源存在差异」。本仓库的 metadata 有版本字段，但矛盾处理规则未实现——诚实标注为设计空间。' },
    ],
  },
  {
    id: 'eval',
    title: 'Eval 体系：Hit@k / MRR / 引用忠实',
    intro: '评测是 RAG 的尺子：没有它，一切「优化」都无法验收，一切排期都是拍脑袋。本仓库把评测做成了 CLI 黄金集（docs/golden-eval-v0.1-dev.md），这一章讲指标怎么读、集怎么建、以及它如何决定投入顺序。',
    sections: [
      {
        heading: '三层指标：检索、生成、安全',
        paragraphs: [
          '检索层——Hit@k：正确证据出现在前 k 条的比例，回答「捞没捞到」；MRR：正确证据排名倒数的均值，回答「捞到后排得好不好」。两个合读就是诊断表：Hit@20 高 + Hit@5 低 → 池里有货排序差 → 上重排；Hit@20 就低 → 召回根子坏 → 修切分/嵌入/混合，上重排没用。这两个指标直接决定了本仓库「先评测、再混合、再重排」的排期逻辑。',
          '生成层——引用忠实率：答案中每个 [S#] 句是否真被所引证据支持（人工抽检起步，模型辅助判断也要再抽检模型本身）；引用覆盖率：事实句里带引用的比例。这两个是对外承诺的核心：「98% 可溯源」远比「答得很准」可验证。',
          '安全层——该拒答时拒答率（高风险问题被安全门拦下的比例）与误拦率（正常问题被错杀的比例）。两者是跷跷板，都要在评测集里有专门用例——本仓库黄金集里有安全用例这一类。',
        ],
      },
      {
        heading: '黄金集怎么建（PM 亲自干的活）',
        paragraphs: [
          '规模：20-50 条起步就够改变决策质量——完美是敌人。构成：真实用户问法（含错字与口语）、每条标注「应命中的 chunk/文档」与「应引用的来源」、覆盖主要问题类型 + 边角（应拒答的、库里没有的、跨文档的）。',
          '维护：评测集是活的——上线后每月从真实坏 case 里补充；语料大版本更新时同步校订标注。评测集偏离真实流量分布时，指标会给出虚假的安全感。',
          '本仓库现状：golden eval 以 CLI 存在并有结果文档；工作台里没有评测页。理想闭环是「改配置 → 保存草稿 → 一键跑黄金集 → 对比两个 Recipe 的 Hit@5/忠实率」——这是我最想补的下一个工作台功能，因为它把「装配自由」升级为「装配即验证」。',
        ],
      },
      {
        heading: '评测驱动的排期：把指标翻译成 roadmap',
        paragraphs: [
          '这套翻译是 PM 在 RAG 团队里的核心杠杆：Hit@20 低 → 排期给数据面（切分、嵌入、混合检索）；Hit@20 高 Hit@5 低 → 排期给重排；忠实率低 → 排期给生成约束与校验；该拒答不拒答 → 排期给安全门与证据评分。每个季度的算法投入都应该能指着评测曲线说「因为这条线，所以做这个」。',
        ],
      },
    ],
    interviewQs: [
      { q: '没有标注预算怎么起步？', a: '三条低成本路径：自己标 20 条（一天工作量，够用）；用 LLM 从语料反向生成「问题-出处」对再人工校验（比从零标快 5 倍）；从客服日志里挑高频真实问题。关键是先有尺子再谈优化——尺子糙一点也比没有强。' },
      { q: '线上怎么持续监控质量？', a: '离线黄金集管回归（每次改配置跑一遍），线上管信号：用户点踩率、转人工率、检索零命中率、降级发生率（本仓库的 Prometheus 降级计数器就是为此准备的）。离线保下限，线上找盲区，坏 case 回流评测集形成闭环。' },
    ],
  },
]
