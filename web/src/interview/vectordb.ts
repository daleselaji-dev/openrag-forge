import type { DeepDiveChapter } from './types'

// 向量库专章：按「面试能讲 8-12 分钟」的深度写。
// 核心 PM 观点：向量库是可重建的派生索引，不是真相源——本仓库用 SQLite(真相) + Qdrant(派生) 落实了这一点。

export const VECTORDB_CHAPTER: DeepDiveChapter = {
  id: 'vectordb',
  title: '向量库专章：选型、原理、角色与设计债',
  intro: '这一章是必修课：向量库是 RAG 里最容易被神化也最容易被误用的组件。我按面试口述的顺序组织——先立观点（派生索引而非真相源），再讲原理（空间、ANN、过滤），再对比选型，最后诚实交代本仓库的设计债。文末有 8-12 分钟口述提纲。',
  sections: [
    {
      heading: '一、先立观点：向量库是派生索引，不是真相源',
      paragraphs: [
        '我对向量库的第一条产品判断：它是「可以随时炸掉重建」的派生物，永远不是数据的家。本仓库的落地：SQLite 存原文、Block、Chunk、metadata、版本号与 SHA-256——这是真相源；Qdrant 里只有向量 + payload 副本，任何时刻都可以从真相源全量重建（工作台「数据」页的「重建索引」按钮就是这个操作的一键化）。',
        '为什么这个架构观很强？三个理由。第一，嵌入模型必然会换：换模型 = 所有向量作废重算，如果向量库是真相源，「升级模型」就成了「迁移全部数据」的惊险手术；而在派生架构里它只是「重建一次索引」的例行操作。第二，故障半径可控：Qdrant 挂了，系统降级为词法检索（本仓库的 lexical_fallback 真实演示了这条路径），文档零丢失，恢复 = 重建；如果真相在向量库里，挂了就是数据事故。第三，审计与合规：监管问「你们的原始资料在哪、改没改过」，答案必须指向一个带版本与哈希的权威存储，而不是一堆浮点数。',
        '事故恢复叙事（面试可直接讲）：假设周五晚 Qdrant 数据目录损坏。派生架构下的 runbook 是：① 系统自动降级词法检索，服务不中断只降质，Trace 与指标（降级计数器）立刻可见；② 起新 Qdrant 实例；③ 调重建接口，从 SQLite 按 chunk 全量重嵌入重写入；④ 健康探针恢复 ready，降级计数归零。全程没有「数据找回」环节——因为向量本来就不是数据，是数据的影子。',
      ],
    },
    {
      heading: '二、逻辑基础：embedding 空间与相似度度量',
      paragraphs: [
        '向量库存的是嵌入模型输出的定长向量（本仓库由所配 Embedding 模型决定维度，常见 384~3072 维）。「语义相似」被翻译成「空间中距离近」——这个翻译的质量完全由嵌入模型决定，向量库只负责「快速找最近的」。这就是为什么我坚持：查询侧与索引侧必须同一个模型，不同模型的向量空间没有可比性，混用后分数即噪声（本仓库把 dense_retrieve 的 model_ref 标为「不生效」，正是为了防止用户以为能单独换查询侧模型）。',
        '三种度量要能张口就来：cosine（余弦）只看方向不看长度，对归一化后的语义向量最常用，也是本仓库 collection 的默认；dot（内积）在向量已归一化时与 cosine 等价，未归一化时会偏向「长」向量——某些模型（如部分推荐场景）刻意利用这一点；L2（欧氏距离）看绝对位置差。实务结论：跟着嵌入模型的官方推荐走，模型文档说用 cosine 就用 cosine——度量选错是「一切分数都不对但系统不报错」的隐形故障。',
      ],
    },
    {
      heading: '三、ANN 直觉：为什么快、牺牲了什么',
      paragraphs: [
        '精确找最近邻要和全库每个向量算一次距离，百万级就撑不住在线时延，所以生产用 ANN（近似最近邻）——用可控的召回损失换数量级的速度。面试讲直觉不背论文：',
        'HNSW（分层可导航小世界图）：把向量连成多层「高速公路网」——顶层稀疏长边负责快速跨越，底层稠密短边负责精确逼近；查询像「先上高速再下辅路」。特点：查询快、召回高、支持增量插入，是 Qdrant/Weaviate/Milvus/pgvector 的主流默认。代价：全图常驻内存，内存是主要成本；删除是标记式的，攒多了要重建图。两个旋钮值得记住：ef（查询时探索宽度，越大越准越慢）、M（每点连边数，越大图越稠内存越高）。',
        'IVF（倒排文件）：先把空间聚成 nlist 个簇，查询只进最近的 nprobe 个簇里扫。内存友好、适合超大规模离线场景；代价是簇边界效应（答案恰好在邻簇边上会漏），且数据分布漂移后要重训簇中心。',
        'DiskANN（Vamana 图 + SSD）：把图的大部分放 SSD、内存只留压缩草图，十亿级向量单机可查。代价是实现复杂、更新不如 HNSW 灵活——它解决的是「内存放不下」这个成本问题，规模没到别碰。',
        'PM 要点：ANN 参数是「召回率-时延-内存」三角上的滑块。验收向量库时要问的不是「用了什么算法」，而是「在我们的数据规模与目标召回率下，P99 时延和内存账单是多少」。',
      ],
    },
    {
      heading: '四、核心结构：collection / 维度 / payload / point id 对齐 / 删除与重建',
      paragraphs: [
        'collection：一组同维度、同度量的向量集合，类似数据库的表。创建时锁定 vector dim 与 metric——换嵌入模型导致维度变化时，collection 必须重建，没有「原地改维度」这回事。这是「换模型是大事」的物理原因。',
        'point：collection 里的一条记录 = id + 向量 + payload。本仓库的关键纪律是 point id 与 chunk_id 对齐：同一个 Chunk 在真相源和索引里共用身份。这个对齐让三件事成立——重建幂等（重跑不会产生重复点，upsert 按 id 覆盖）、精确删除（删文档 → 按 chunk_id 删点）、证据回链（检索命中 → 按 id 回真相源取全文与 metadata，Trace 里的 chunk_id 能一路追到原文）。没有这条纪律的系统，重建一次索引就多一倍幽灵数据。',
        'payload：挂在 point 上的结构化字段（本仓库放 document_id、heading_path、页码等）。它的价值在过滤：Qdrant 支持带条件的 ANN 检索（预过滤），「只在现行版政策里找最近邻」一次完成——这正是 metadata_filter 节点真实实现时的落点。要给过滤字段建 payload 索引，否则条件一上时延崩。预过滤 vs 后过滤的差别在元数据过滤环节讲过：预过滤保证返回满 k 条合格结果，权限场景必须预过滤。',
        '删除与重建：向量库的删除普遍是「标记删除」，空间回收靠后台优化或重建。所以运营上要有两个习惯：监控已删除点占比；把「全量重建」当成低成本例行操作演练——本仓库一键重建的设计让这个演练变成日常，而不是灾难时第一次做。',
      ],
    },
    {
      heading: '五、选型对比：七个候选怎么挑',
      paragraphs: [
        '选型第一问不是「哪个最快」，而是「我们有没有既有基建、规模多大、谁来运维」。下面按这个逻辑排：',
      ],
      table: {
        columns: ['方案', '定位一句话', '强项', '代价 / 何时不选'],
        rows: [
          ['Qdrant', 'Rust 单体，API 干净，过滤强', '部署轻（单容器起步）、filtered HNSW 成熟、payload 灵活——正是本仓库选它做教学与 Lite 部署的原因', '集群与多租户生态弱于大厂方案；超大规模案例少'],
          ['Milvus', '云原生分布式向量库（存算分离）', '十亿级规模、多索引类型（HNSW/IVF/DiskANN）可选、Zilliz 托管可购', '组件多（etcd/MinIO/Pulsar…）运维重；小场景严重过度'],
          ['Weaviate', '向量库 + 内建模块生态（自动向量化、混合检索）', 'BM25+向量混合内建、GraphQL API、模块化省胶水代码', '被模块生态绑定后迁移成本高；纯向量性能不是最优'],
          ['pgvector', 'Postgres 扩展：向量进关系库', '与业务数据同库同事务，运维零新增、备份复用——中小规模（百万级内）的务实之选', '规模与 QPS 上限低于专用库；HNSW 参数调优空间小'],
          ['Pinecone', '全托管 Serverless 向量服务', '零运维、弹性伸缩、按用量付费——最快到生产', '数据出域；成本随规模爬升；黑盒程度高、不可自托管'],
          ['ES/OpenSearch dense', '倒排引擎加向量字段：一个集群两路召回', '天然混合检索 + 权限/聚合/过滤全家桶；已有 ES 团队边际成本极低', '纯向量性能与内存效率逊于专用库；没有 ES 基建时别为它新建'],
          ['Vespa', '大规模在线多阶段排序平台', '向量+倒排+ML 精排引擎内一体，亿级规模标杆', '学习曲线与运维成本全场最高；知识库问答场景过度设计'],
        ],
      },
    },
    {
      heading: '六、决策树与迁移成本',
      paragraphs: [
        '我的选型决策树（面试可直接说）：① 已有 ES/OpenSearch 团队 → 用 hybrid，别新建向量库（省掉双索引一致性这个长期税）；② 数据不能出域、规模百万级内、已有 Postgres → pgvector，运维零新增；③ 数据不能出域、规模或过滤需求超出 pgvector → Qdrant（轻）或 Milvus（超大规模）；④ 数据可出域、要最快上线 → Pinecone；⑤ 检索排序本身是产品核心竞争力 → 认真评估 Vespa。',
        '迁移成本要拆两半看。便宜的一半：向量本身——派生架构下「迁移」＝「换目标重建」，从真相源重嵌入写入新库即可，不存在向量搬家。贵的一半：查询语义——过滤 DSL、混合检索语法、一致性行为、分页方式各家不同，检索层代码要改写；以及运维肌肉记忆（监控、扩容、故障 runbook）要重建。所以我的建议是：在检索层留一个薄适配接口（本仓库的 adapters/qdrant.py 就是这个意图——目前只有 Qdrant 一个实现，接口边界已留出），但不要为「未来可能迁移」过度抽象。',
        '混合检索时的分工要讲清：向量库负责「语义像」（跨措辞泛化），倒排负责「字面准」（型号、编号、人名）与结构化过滤/聚合。两路各取 Top-N 后用 RRF 按名次融合。「要不要为混合检索专门引入第二个引擎」的答案通常取决于既有基建——这也是 ES hybrid 路线存在的理由。',
      ],
    },
    {
      heading: '七、本仓库的现状与设计债（不藏）',
      paragraphs: [
        '当前真实状态：单一 Qdrant collection（名字固定为配置项），全部知识库共用；没有 KB 级隔离，也没有租户概念。这意味着：多个业务的文档会在同一个向量空间里互相「串味」，权限隔离无从谈起。embed_index 节点的 collection 字段在检查器里标注「不生效」——我把这笔债写在表单上，而不是让用户以为改了就隔离了。',
        '如果偿还这笔债，方案与取舍是：方案 A，每 KB 一个 collection——隔离最干净、可按 KB 独立重建，代价是 collection 数量随 KB 增长、小 KB 资源利用率低；方案 B，单 collection + payload 里加 kb_id + 强制预过滤——资源利用率高，代价是隔离靠查询纪律保证（漏写过滤条件 = 越权），审计要求高的场景不可接受。我的排序：先 B（改动小、验证需求），确认多租户是真需求后迁 A。这类「债务偿还路线图」正是 PM 在架构讨论里该输出的东西。',
        '其他相关债务一并列出：查询侧嵌入模型不可独立切换（防误用的临时限制）；删除文档后的向量清理依赖重建而非实时删除；没有向量库容量与降级率的告警阈值配置。每一条都能在工作台的标注或告警里找到对应痕迹——讲解与产品状态一致，是这个作品集的基本要求。',
      ],
    },
  ],
  talkTrack: [
    { marker: '第 0-1 分钟 · 立观点', points: ['「我先说结论：向量库在我的架构里是可重建的派生索引，不是真相源。」', '一句话交代本仓库落地：SQLite 存原文与 Chunk（带版本和哈希），Qdrant 只存向量+payload，随时可全量重建。'] },
    { marker: '第 1-3 分钟 · 为什么这个观点重要', points: ['换嵌入模型必然发生 → 派生架构把「危险迁移」降为「例行重建」。', '故障半径：Qdrant 挂 → 词法降级不停服 → 重建恢复（讲周五晚事故 runbook）。', '合规：监管要的权威存储必须带版本哈希，不能是一堆浮点数。'] },
    { marker: '第 3-5 分钟 · 原理速讲', points: ['嵌入空间：相似=距离近，质量由嵌入模型决定，查询/索引必须同模型。', '度量三选一：cosine/dot/L2，跟嵌入模型官方推荐走。', 'ANN 直觉：HNSW=多层高速公路（快、吃内存）、IVF=先分簇再扫（省内存、边界效应）、DiskANN=图放 SSD（十亿级）。', '一句总结：ANN 参数是召回率-时延-内存三角上的滑块。'] },
    { marker: '第 5-7 分钟 · 核心结构', points: ['collection 锁维度和度量 → 换模型=重建，物理上没有原地改。', 'point id 与 chunk_id 对齐 → 重建幂等、精确删除、证据回链三件事的地基。', 'payload 预过滤 vs 后过滤：权限场景必须预过滤。'] },
    { marker: '第 7-10 分钟 · 选型决策树', points: ['先问基建：有 ES 团队 → hybrid；有 PG 且百万级内 → pgvector；不能出域且规模大 → Qdrant/Milvus；能出域求快 → Pinecone；检索即核心 → Vespa。', '迁移成本拆两半：向量免费（重建即迁移），查询语义与运维肌肉贵。', '混合分工：向量管语义像，倒排管字面准，RRF 按名次融合。'] },
    { marker: '第 10-12 分钟 · 诚实收尾', points: ['主动交代设计债：单 collection、无 KB 隔离、collection 字段标注不生效。', '给偿还路线：先 payload+kb_id 预过滤验证需求，再迁每 KB 一 collection。', '收束到方法论：「我对每个组件都问三个问题——它的真相源在哪、挂了怎么降级、换掉它要付什么。向量库只是这套方法论的必修示例。」'] },
  ],
  interviewQs: [
    { q: '为什么不用 Milvus/Pinecone 而选 Qdrant？', a: '按决策树走：这是个单机可跑、要给人拆开看的教学与 Lite 框架——Qdrant 单容器起步、API 干净、filtered HNSW 成熟，运维面最小。Milvus 的分布式组件对本场景是纯负担；Pinecone 数据出域且黑盒，与「可证明」的项目立意直接冲突。选型没有绝对优劣，只有与场景约束的匹配。' },
    { q: '向量库挂了用户会看到什么？', a: '不会看到 500。dense_retrieve 降级为词法重叠检索，Trace 里 backend 显示 lexical_fallback，Prometheus 降级计数器 +1，顶栏 QDRANT 芯片变 warn。答案质量下降但服务连续，且「降级中」这个事实对运营完全可见——降级不可耻，不可见才可耻。' },
    { q: '十亿级向量你会怎么做？', a: '先质疑需求：十亿向量通常意味着该做数据分层（热数据 HNSW 在线、冷数据 DiskANN/IVF 降级服务）与业务分片，而不是找一个「什么都能装」的库。技术选项是 Milvus/Vespa/DiskANN 路线，但 PM 的第一贡献是把「十亿」拆成「哪些必须毫秒级、哪些可以百毫秒级」。' },
  ],
}
