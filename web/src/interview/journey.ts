import type { JourneyGeneration } from './types'

// 设计历程：以本仓库真实提交史为底稿的「作品叙事」。
// 这是候选人（我）的设计历程，不是厂商宣传——每一代都写清牺牲了什么。
// 提交锚点（git log）：89ee9bf bootstrap → 8228648/a343311 可装配 →
// 0b26f10/0cd3d97 生产横切 → 9791622/b9a0f09 诚实标注 Control Room。

export const JOURNEY_INTRO: string[] = [
  '下面这条时间线是我做这个 RAG 项目的真实决策顺序，不是回头补写的漂亮故事。每一代我都会讲四件事：当时的业务动机、我做的产品决策、为此牺牲了什么、以及为什么必须走到下一代。',
  '一个提前声明：这条线走到今天，运维横切面（可观测、探针、鉴权、限流）已经达到生产级要求，但检索算法面仍然只有稠密检索是真实现——稀疏检索、RRF、重排在目录里存在、在执行器里是占位。我把这个差距明确标在工作台上（占位/退化徽标），因为「知道自己没做什么」本身就是这个作品最想展示的产品能力。',
]

export const JOURNEY: JourneyGeneration[] = [
  {
    id: 'v0',
    name: 'V0 最简形态：整篇文档塞进上下文',
    tagline: '没有索引、没有检索——问题 + 全文直接进 Prompt。',
    motivation: [
      '起点问题很朴素：客服知识库就几十篇 FAQ，用户问一句，能不能直接把相关文档全文和问题一起丢给模型？我先验证「模型 + 文档」这条路本身通不通，再谈架构。',
      '这一步是纸面原型 + 脚本验证，仓库里没有对应 Recipe——我不假装它存在。但它决定了后面所有设计：我亲眼看到了这条最简路径在什么规模上断掉。',
    ],
    decisions: [
      '产品决策只有一个：什么都不建。不建索引、不切分、不管理版本，最快速度拿到「答案质量的上限感受」。',
      '同时定下一条底线指标：回答里的每个事实必须能指回原文——这条要求从 V0 一直活到今天的 [S#] 引用体系。',
    ],
    tradeoffs: [
      '牺牲了规模：上下文窗口装不下第 30 篇文档之后的内容，模型开始「捡最前面的说」。',
      '牺牲了成本：每次请求都把全库 token 烧一遍，问 10 个问题的钱够建一次索引。',
      '牺牲了可追责：模型说「根据文档」，但没人知道是哪篇哪段——出错时无法归因。',
    ],
    whyNext: '文档超过上下文窗口的那一天，这条路物理性地断了。不是优化问题，是必须引入「先检索后生成」的结构问题——这就是 V0.1。',
    interviewQs: [
      { q: '现在长上下文模型动辄百万 token，V0 这条路是不是又活了？', a: '部分场景活了，但三个约束没变：成本随文档量线性上涨；「大海捞针」位置偏差依然存在；最关键的是无法逐条引用归因。我的判断是：文档量小、更新频繁、不要求审计的场景可以直接长上下文，但企业知识库三条全踩，仍然需要检索。' },
      { q: '你怎么判断该从 V0 升级？', a: '我看三个信号：文档总 token 超过窗口的 50%（留出回答与指令空间）、单次请求成本超过可接受阈值、以及业务方第一次问「这个答案依据是哪篇文档」。第三个信号最早出现。' },
    ],
    legacy: ['工作台里没有 V0 的 Recipe——这是有意的诚实：不存在的东西不上画布。它的遗产是「回答必须可引用」这条要求，你在任何一次真实运行的「回答与证据」页都能看到 [S#] 引用。'],
    actions: [{ label: '看 [S#] 引用长什么样（先运行一次）', bottomTab: 'result' }],
  },
  {
    id: 'v0_1',
    name: 'V0.1 Naive RAG：切分 + 向量召回 + 生成',
    tagline: '最小可用的检索增强：Chunk → Embedding → Top-K → Prompt。',
    motivation: [
      '业务动机：文档量破了上下文窗口，且成本不可持续。我需要一条「只把相关的几段送进模型」的路。',
      '这一代对应画布上的 v0_1_dense Recipe：question → dense_retrieve → context_builder → llm_generate → policy_gate，是今天仍然真实跑通的主链路。',
    ],
    decisions: [
      '选稠密检索而不是关键词检索起步：客服问法五花八门（「莫名扣了我的钱」vs「陌生扣款」），语义召回对措辞差异的容忍度是产品体验的下限。',
      '定了检索单位：Chunk（约 1200 字符、120 重叠）而不是整篇文档——这是引用粒度与召回精度的平衡点，可以在 Chunker 节点里调。',
      '砍掉了当时能想到的一切「高级」：不做混合检索、不做重排、不做查询改写。先让一条最短的链稳定跑通、可测量。',
    ],
    tradeoffs: [
      '牺牲了关键词精确匹配：产品型号、法条编号这类 token 级匹配，纯向量会输给 BM25——这个坑我留到后面（至今是诚实的欠账）。',
      '牺牲了召回质量的上限：Top-K 直接进 Prompt，没有精排，排在第 4、5 位的噪声会稀释上下文。',
      '换来的是：整条链路每一步可解释——问题向量化、近邻检索、拼装、生成，出问题能定位到环节。',
    ],
    whyNext: 'Naive RAG 跑通后，最大的痛不是效果而是「说不清」：为什么这个问题答错了？是没召回、召回错、还是生成瞎编？没有 Trace 之前，每次排查都靠猜。可观察性成了下一代的主题。',
    interviewQs: [
      { q: 'top_k 怎么定？为什么默认 5？', a: '这是三方权衡：k 太小漏证据（Hit@k 下降），k 太大稀释上下文且拉长 Prompt（成本+噪声）。5 是我在没有重排的前提下的保守值——如果有真实重排，正确姿势是「粗召回 k=50，精排出 6」。工作台里 dense_retrieve 节点的 top_k 是真实生效的，可以现场改了跑对比。' },
      { q: '为什么不一步到位上混合检索？', a: '产品阶段判断：当时没有任何评测基线，我无法证明混合检索带来的提升值得两套索引的运维成本。先把单路跑通、建立评测（后来的 golden eval），有了尺子再加东西。事实上这个仓库到今天稀疏检索仍是占位——我宁可标注「没做」也不做一个测不出收益的半成品。' },
    ],
    legacy: ['左轨「装配」页的 V0.1 Dense baseline 就是这一代本尊，至今是唯一全 live 的检索链路。', '选中 dense_retrieve 节点：top_k、score_threshold 真实生效；选中 chunker 节点：max_chars/overlap 真实生效。'],
    actions: [{ label: '加载 V0.1 Dense baseline', recipeId: 'v0_1_dense', railTab: 'recipe' }],
  },
  {
    id: 'v0_2',
    name: 'V0.2 可观察：Recipe 编译 + Trace + Evidence Capsule + 解析路由',
    tagline: '从「能跑」到「能证明」：每次运行都留下可审计的证据链。',
    motivation: [
      '业务动机：业务方问「上周三那个错误答案是怎么来的」，我答不上来——配置改过、文档更新过、模型换过，无法复现。这在企业场景是致命的：不可复现 = 不可信。',
      '同期第二个痛点在数据面：PDF、Office、CSV 混着上传，解析质量参差，坏解析悄悄污染索引，两周后才在坏答案里暴露。',
    ],
    decisions: [
      '把流水线从代码升格为数据：Recipe 是带类型端口的 DAG，编译器校验端口兼容与无环，产出 SHA-256 内容哈希。发布版不可变——「哪个版本的流水线回答了这个问题」从此有了唯一答案。',
      '每个节点执行都写 Trace 事件（输入摘要、输出摘要、状态），整次运行连同问题、Recipe 哈希、证据引用、安全决策打包成 Evidence Capsule JSON——审计不用信截图，直接读胶囊。',
      '数据面加解析路由：按文件签名选解析器，决策带置信度与 reason_codes，Ingest Trace 让「文档是怎么进来的」和「问题是怎么答的」一样可查。',
    ],
    tradeoffs: [
      '牺牲了灵活性：想加一种节点必须先注册端口类型，编译器会拒绝野连线。写代码的人觉得烦，但这正是产品要的——画布上的结构就是运行时的结构，不存在「图上一套、代码另一套」。',
      '牺牲了性能余量：每步都记 Trace 有开销。我的取舍是业务 Trace 永久落 SQLite（审计需要），性能瀑布交给后来的 OTel（采样可调）。',
      '有限纠错循环被编译器强制显式化：环必须用 bounded_corrective 声明重试上限——无界自纠错是 RAG 生产事故的常客，我直接在编译期禁掉。',
    ],
    whyNext: '可观察解决了「事后能查」，但链路本身还是工程师用代码拼的。业务方想试「换个切分粒度」「加个过滤」得排我的开发排期——流水线的装配权需要交出去，这就是 V0.3。',
    interviewQs: [
      { q: 'Recipe 哈希解决什么问题，为什么不用版本号？', a: '版本号是人赋予的，会撒谎（改了内容忘了升版本）；内容哈希是结构本身算出来的，改任何节点配置哈希必变。复现的完整锚点是：Recipe 哈希 + 文档版本 + 模型 ID，三者都在 Evidence Capsule 里。' },
      { q: 'Trace 记这么细，隐私和存储怎么办？', a: '诚实回答：当前 Trace 记录输入输出摘要而非全文，但没有做 PII 脱敏和保留期策略——这是接真实客户数据前必须补的合规项，我把它列在生产就绪告警里，而不是假装已解决。' },
    ],
    legacy: ['画布顶部工具栏的 hash 前缀、「校验」按钮的编译报错、底部「运行 Trace」和「Ingest Trace」页签、结果页的「下载 Evidence Capsule」——全部是这一代的遗产。', '上传任意文档后看 Ingest Trace：route 一步的置信度与 reason_codes 就是解析路由的决策记录。'],
    actions: [{ label: '看运行 Trace', bottomTab: 'trace' }, { label: '去上传文档看 Ingest Trace', railTab: 'data', bottomTab: 'ingest' }],
  },
  {
    id: 'v0_3',
    name: 'V0.3 可装配：画布 + 节点配置 + 模型注册 + 场景',
    tagline: '把流水线的装配权从工程师手里交给使用者。',
    motivation: [
      '业务动机：每个「试试看」都要排开发排期，迭代速度被我卡住。既然 Recipe 已经是数据（V0.2 的遗产），那编辑数据就不需要写代码——需要的是一个装配界面。',
      '第二个动机是模型解耦：本地 LM Studio、内网 vLLM、云端 API 来回切，模型配置散在环境变量里换一次疼一次。',
    ],
    decisions: [
      'React Flow 画布：拖节点、连线、删边，保存时走同一个编译器——画布不是示意图，是真实的装配台，连错端口会被拒绝。',
      '模型注册表：Chat / Embedding / Reranker 模型注册为档案（base_url + model_name），节点用 model_ref 引用。换模型 = 换绑定，不改代码。附带 probe 探测端点可达性。',
      '场景卡片：把「客服质检」「政策问答」做成可导入的场景预设——声明业务问题、所需资料、默认 Recipe、该观察的 Trace。这是给非工程角色的入口。',
      '内置 Recipe 发布版不可变：在发布版上改配置会自动 fork 草稿副本——保护基线不被试验污染。',
    ],
    tradeoffs: [
      '牺牲了「界面自由」：画布只能摆注册过的节点类型、连类型兼容的线。比通用白板难受，但换来「画出来的一定能编译」。',
      '埋下了一个日后必须还的债：目录里为了展示演进路线放了 V0.2~V0.8 的进阶 Recipe（混合、重排、纠错、图谱），但执行器没有跟上——目录超前于执行器。这个债在「当前诚实态」一代才用徽标体系还清。',
    ],
    whyNext: '可装配让试验变快，但要把它当真东西部署，缺的是完全不同的一类能力：探针、鉴权、限流、结构化日志、真实耗时。下一代不加任何算法，专门补运维横切面。',
    interviewQs: [
      { q: '低代码画布很容易做成玩具，你怎么避免？', a: '一条原则：画布和运行时共享同一个编译器和同一份节点目录。画布上保存 = 服务端编译，编译失败就保存失败。没有「导出再翻译」的中间层，就没有图与实之间的漂移。' },
      { q: '把装配权交给业务方，怎么防止他们配出糟糕的链路？', a: '三层防护：编译器挡结构错误（类型、环）；发布/草稿机制挡误改基线；Trace + 场景观察清单让「配得不好」可测量而不是靠感觉。挡不住的是业务判断错误——那本来就该由评测数据说话。' },
    ],
    legacy: ['整个中间画布、右侧检查器的结构化表单、左轨「模型」页的注册与 probe、「场景」页的卡片，都是这一代。', '现在就可以验证：拖一条类型不兼容的连线，编译器会拒绝。'],
    actions: [{ label: '去模型注册页', railTab: 'model' }, { label: '去场景库', railTab: 'scenario' }],
  },
  {
    id: 'v0_4',
    name: 'V0.4 生产横切：OTel + 探针 + 鉴权 + 真实耗时',
    tagline: '不加一个算法节点，把「原型」和「服务」之间的运维鸿沟填平。',
    motivation: [
      '业务动机：要把工作台放到内网给真人用，第一天就会被问四个问题：挂了怎么知道（探针）、慢在哪（追踪）、谁能访问（鉴权）、被打爆怎么办（限流）。这些与 RAG 算法无关，但决定它是不是一个「服务」。',
      '我刻意让这一代零算法改动——横切面和算法面是两条独立的成熟度轴，混在一起汇报会骗到自己。',
    ],
    decisions: [
      '双层追踪：业务 Trace（审计视角，永久 SQLite）+ OpenTelemetry span（性能视角，采样导出 Jaeger），同一 trace_id 互查；响应头 X-Trace-Id 直接可粘贴检索。',
      'JSON 结构化日志自动携带 request_id/trace_id；Prometheus 指标包含降级次数计数器——「今天词法回退了多少次」是一条能报警的曲线。',
      '/livez /readyz 探针、优雅关机、出站连接池与显式超时、SQLite WAL、API Key（常数时间比较）、服务级限流、安全响应头、非 root 容器。',
      'Trace 的 duration_ms 从占位值改为真实测量——假的耗时比没有耗时更有害，它让人做出错误的性能判断。',
    ],
    tradeoffs: [
      '牺牲了功能进度：这一整代没有任何用户可感知的新能力，纯「看不见的工程」。作为 PM 我认为排期上这是对的：可观测性欠账越晚还越贵。',
      '限流做在服务级中间件而不是节点级：画布上的 rate_limit 节点至今是占位——真实生效的是 OPENRAG_RATE_LIMIT_PER_MINUTE。我在节点上标注了这一点，防止画布骗人。',
    ],
    whyNext: '横切面达标后，最后的问题反而是「诚实」本身：目录里的进阶节点（稀疏/RRF/重排）会让画布看起来比实际能力强。下一代不写新功能，写「承认」。',
    interviewQs: [
      { q: '业务 Trace 和 OTel span 为什么要两套，不冗余吗？', a: '受众不同、生命周期不同。业务 Trace 回答「这个答案的证据链是什么」，要永久保存供审计；OTel 回答「这 3 秒慢在哪」，采样保留、过期即弃。用同一个 trace_id 关联，但绝不合并——合并的结果是审计数据被采样丢掉，或者性能系统被审计数据撑爆。' },
      { q: '这一代之后你敢接生产流量吗？', a: '运维意义上敢：挂了知道、慢了能查、有鉴权限流。但业务意义上不敢：检索算法还是 Naive 级、安全门只是关键词正则、没有 KB 隔离。我的结论是 3/10 的生产就绪度——横切面拉高不了算法面的分。' },
    ],
    legacy: ['顶栏 READINESS 告警芯片（production_readiness 字段）、运行元信息里的 req/otel trace id、Trace 每行的真实 duration_ms 与耗时条。', 'docs/observability.md 与 docs/production-checklist.md 是这一代的文字版。'],
    actions: [{ label: '运行一次看真实耗时', bottomTab: 'trace' }],
  },
  {
    id: 'current',
    name: '当前诚实态：Control Room + live/stub 徽标，算法面仍是 Naive+',
    tagline: '把「已实现」和「仅声明」的边界画在界面上，作为产品能力交付。',
    motivation: [
      '最后一个要解决的产品问题是自己制造的：V0.3 的目录超前于执行器，画布上摆着 reranker、rrf_fusion，不知情的人会以为混合检索已经在跑。演示时含糊过去很容易——但这个项目的立身之本是「可证明」，界面撒谎等于全盘作废。',
      '于是这一代的核心交付是一套诚实标注体系，而不是新算法。',
    ],
    decisions: [
      '节点目录增加 implemented 三态：live（真实执行）/ fallback（退化为共享路径）/ stub（占位直通）。画布徽标、检查器徽章、Trace 的 execution 列三处同源展示。',
      '配置表单字段级标注 effective：改 reranker 的 candidate_k 会保存但明确提示「不生效」——表单不骗人。',
      '全高 Control Room 布局：左轨（装配/数据/模型/场景）+ 画布 + 检查器 + 底部 Trace；辅助教学做成可开关的模式而不是常驻噪声。',
      '现在这个「面试讲解」模式是同一思路的延伸：讲解跟着节点走、跟着 Trace 走，而不是一份脱离工作台的文档。',
    ],
    tradeoffs: [
      '牺牲了演示观感：画布上一片「占位」徽标不好看。但换来的是每个结论都能当场用 Trace 验证——对面试和对客户，这都是更强的立场。',
      '诚实清单（当前真实边界）：稀疏/RRF/重排/意图路由/元数据过滤/上下文构建器/纠错/缓存均为占位或退化；安全门是关键词正则；Qdrant 单 collection、无 KB 隔离；Postgres/MinIO/Celery 适配器未实现。',
    ],
    whyNext: '下一步的优先级排序（如果继续投入）：① 真实稀疏检索 + RRF（先建评测对照）；② KB 隔离（多租户的地基）；③ 真实重排（精度收益最大的单点）；④ 元数据过滤（政策类场景的合规刚需）。排序依据是「评测能证明收益 + 解锁的场景数」。',
    interviewQs: [
      { q: '你给自己的项目打 3/10，不怕被面试官觉得项目不行吗？', a: '恰恰相反，这个分数是拆开打的：运维横切面按生产 checklist 逐项达标，算法面诚实地停在 Naive+。能精确说出自己在哪条轴的哪个位置、下一步投什么、为什么，比一句「我做了企业级 RAG」可信得多。评估一个 RAG 产品的成熟度本来就是 PM 的核心技能。' },
      { q: '怎么证明徽标说的是真话？', a: '现场证明：跑一次 v0_1_dense 和一次 v0_4_rerank，对比两次的证据列表——顺序完全一致，因为 reranker 是直通的；Trace 里那一行明确写着 stub_passthrough。徽标、Trace、行为三方一致，这就是「可证明」。' },
    ],
    legacy: ['你现在看到的一切：画布徽标、检查器的生效/不生效、Trace 的 execution 列、顶栏三态模式切换。', '实验手册里的「占位证明实验」可以一键复现上面那个对比。'],
    actions: [{ label: '加载 v0_4_rerank 做占位证明', recipeId: 'v0_4_rerank', railTab: 'recipe' }, { label: '看实验手册', bottomTab: 'trace' }],
  },
]
