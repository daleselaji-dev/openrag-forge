# Contributing

Keep the core provider-agnostic and traceable. New parsers, retrievers and policies must expose a stable plugin descriptor, deterministic tests and a failure mode. Do not add a framework dependency when a small adapter is sufficient. Any benchmark claim must include the dataset snapshot, recipe hash and the actual report.

